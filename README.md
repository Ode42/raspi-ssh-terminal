# Raspberry Pi ssh terminal emulator
Connect multi-server via ssh in Raspberry Pi

## Dependencies
Cargo rust-environment

## installation
clone repository and build it with 'cargo build --your target--